# Chapter 7: [Chapter Title]

[Replace this with the seventh chapter of *The Sovereign AI Company*.

Rename this file to match the chapter title (e.g. `07 - Your Real Title.md`)
and update the corresponding entry in `scripts/build-epub.py` and
`scripts/build-pdf.py` in the `CHAPTERS` list.

Markdown conventions used across the book:

- `# Chapter N: Title` — chapter heading (exactly one per file)
- `## Section heading` — top-level sections within a chapter
- `### Subsection heading` — further nesting if needed
- `**bold**` and `*italic*` for emphasis
- `> quote` for block quotes — rendered with a left border in EPUB and
  indented italic in PDF

Sources cited in the body get added to `content/back-matter/references-sources.md`
under the matching `## Chapter N: Title` heading.]
